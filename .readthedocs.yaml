version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  jobs:
    pre_install:
      - echo "rsm_build_prod = True" >> docs/source/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
