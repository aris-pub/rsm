# Sidebar Demo

:toc: {
  :view: tree
}
::

## Introduction
  {:label: sec-intro}

:notation:
  \eig $\lambda$ eigenvalue
::

We :write: $\eig$ for the spectral radius :: throughout this demonstration document.

This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.
This paragraph is filler so the proof below starts off-screen on load.

## Results
  {:label: sec-results}

:theorem: {:label: thm-x} A claim about $\eig$.::

:proof:
  :step: :assume: $\eig$ is the spectral radius of :ref:thm-x::.::
  :step: {:label: st-1} First, $\eig$ is real.::
  :step: {:label: st-2} Then it is bounded, by :ref:st-1::.::
  :step: :qed: Done by :ref:st-2::.::
::

Trailing prose so the proof can scroll fully into the reading band.
Trailing prose so the proof can scroll fully into the reading band.
Trailing prose so the proof can scroll fully into the reading band.
Trailing prose so the proof can scroll fully into the reading band.
Trailing prose so the proof can scroll fully into the reading band.
