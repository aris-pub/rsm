# Notation

:notation:
  \eig $\lambda$ general eigenvalue
  \edges $e(#1)$ number of edges
::

Inline use of the macro: $\eig$.

$$
\eig + 1
$$

Parameterized macro inline: $\edges{G}$.

$$
\edges{G} = \edges{H}
$$
